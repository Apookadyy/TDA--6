name: Enterprise CI

on: [push]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Install
        run: pip install flask pytest requests
      - name: Run Tests
        run: pytest